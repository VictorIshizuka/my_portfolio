import styles from "./Footer.module.css";

//import ReactComponent from "../../assets/marca_registrada.svg";

export const Footer = (): JSX.Element => {
  return (
    <footer className={styles.footer}>
      {/* <ReactComponent /> */}
      Desenvolvido por Victor
    </footer>
  );
};
